#include "reduce.h"

void reduce(struct reduce_t* this, struct stream_t* stream, void* cb_ptr) {
    // size_t index = 0L;
    // stream->state = SS_INUSE;
    // while(stream->length) {
    //     char* item_ptr = next_item_from_stream(stream);
    //     if(item_ptr) {
    //         this->apply(item_ptr, (const size_t) (index++), cb_ptr, this->data.result_ptr);
    //         free(item_ptr);
    //     }
    // }
}